<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wen.vote.mapper.AppMemberMapper">
    
    <resultMap type="AppMember" id="AppMemberResult">
        <result property="memberId"    column="member_id"    />
        <result property="username"    column="username"    />
        <result property="password"    column="password"    />
        <result property="nickname"    column="nickname"    />
        <result property="phone"    column="phone"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="voteStatus"    column="vote_status"    />
    </resultMap>

    <sql id="selectAppMemberVo">
        select member_id, username, password, nickname, phone, status,vote_status, create_time from olvs_app_member
    </sql>

    <select id="selectAppMemberList" parameterType="AppMember" resultMap="AppMemberResult">
        <include refid="selectAppMemberVo"/>
        <where>  
            <if test="username != null  and username != ''"> and username like concat('%', #{username}, '%')</if>
            <if test="nickname != null  and nickname != ''"> and nickname like concat('%', #{nickname}, '%')</if>
            <if test="phone != null  and phone != ''"> and phone = #{phone}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectAppMemberByMemberId" parameterType="Long" resultMap="AppMemberResult">
        <include refid="selectAppMemberVo"/>
        where member_id = #{memberId}
    </select>

    <insert id="insertAppMember" parameterType="AppMember" useGeneratedKeys="true" keyProperty="memberId">
        insert into olvs_app_member
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null">username,</if>
            <if test="password != null">password,</if>
            <if test="nickname != null">nickname,</if>
            <if test="phone != null">phone,</if>
            <if test="status != null">status,</if>
            <if test="voteStatus != null">vote_status,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null">#{username},</if>
            <if test="password != null">#{password},</if>
            <if test="nickname != null">#{nickname},</if>
            <if test="phone != null">#{phone},</if>
            <if test="status != null">#{status},</if>
            <if test="voteStatus != null">#{voteStatus},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateAppMember" parameterType="AppMember">
        update olvs_app_member
        <trim prefix="SET" suffixOverrides=",">
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="voteStatus != null">vote_status = #{voteStatus},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where member_id = #{memberId}
    </update>

    <delete id="deleteAppMemberByMemberId" parameterType="Long">
        delete from olvs_app_member where member_id = #{memberId}
    </delete>

    <delete id="deleteAppMemberByMemberIds" parameterType="String">
        delete from olvs_app_member where member_id in 
        <foreach item="memberId" collection="array" open="(" separator="," close=")">
            #{memberId}
        </foreach>
    </delete>
</mapper>